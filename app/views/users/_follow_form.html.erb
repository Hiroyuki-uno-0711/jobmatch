
<% if user.followed_by?(current_user) %>

  <%=link_to "フォロー済み", user_relationships_path(user.id), method: :delete, remote: true, class: "btn btn-danger btn-lg", :style=> "width:160px;" %>

<% else %>

  <%=link_to "フォローする", user_relationships_path(user.id), method: :post, remote: true, class: "btn btn-primary btn-lg", :style=> "width:160px;" %>

<% end %>