

  <% if job_information.favorited_by?(current_user) %>

    <%= link_to "興味を取り消す", job_information_favorites_path(job_information), method: :delete, remote: true, class: "btn btn-danger btn-lg", :style=> "width:30%;" %>

  <% else %>

    <%= link_to "興味がある", job_information_favorites_path(job_information), method: :post, remote: true, class: "btn btn-primary btn-lg", :style=> "width:30%;" %>

  <% end %>